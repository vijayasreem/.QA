//TestUserStory.js
({
    doInit : function(component, event, helper) {
        //Initialize component data
        helper.initData(component);
    },
    handleOnClick : function(component, event, helper) {
        //Handle OnClick Action
        helper.handleOnClick(component, event);
    }
})